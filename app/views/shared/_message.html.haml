.message
  .message--info
    .message__username
      #{message.user.name}
    .message__data
      #{message.created_at.strftime('%Y/%m/%d %H:%M')}
  .message__sentence
    %p.sentence
      #{message.body}
